// Learned logging in the console
const greet = "Hello!";

console.log(greet);

//Learned stuffs with interfaces
interface User {
    name: string,
    id: number
};

const user: User = {
    name: "mario",
    id: 5
}